name: Tagger

on:
  create:
    tags:

jobs:
    movetag:
      runs-on: ubuntu-latest
      steps:
      - name: Advanced tagger. Moves major tag to latest minor tag
        uses: actions/github-script@v3
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}          
          script: |
            var re = /refs\/tags\/(v(\d(\.\d*)*))/i;
            var ref = process.env['GITHUB_REF'];
            var match = ref.match(re);
            console.log(re);
            console.log(ref);
            console.log(match);
            return;
            if (match.length == 0)
              return;
            let tag_name = match[0],
              tag_ver = match[1];
            console.log(match, tag_name, tag_ver);
            //: 'refs/tags/v0.1.1',
            //console.log("Current tag name: ",  process.env);
            //console.log(context.repo);
            /*try {
                await github.git.deleteRef({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  ref: "tags/nightly"
                })
            } catch (e) {
              console.log("The nightly tag doesn't exist yet: " + e)
            }
            await github.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: "refs/tags/nightly",
              sha: context.sha
            })*/
